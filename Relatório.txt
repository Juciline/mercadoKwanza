RELATÓRIO DE ERROS E RESOLUÇÕES — MercadoKwanza






















Erro após gerar o ficheiro .sql no MysqlWorkbench

1. Encontramos erros sim (linha 8), o FOREIGN_KEY_CHECKS CONCATENADO
que corresponde a uma Sintaxe inválida com isso o MySQL rejeita a instrução.
A solução foi separar para SET FOREIGN_KEY_CHECKS = 0;

2. ALL sp_inserir_vendas() na linha (405), a chamada à 
procedure depois de ela já ter sido eliminada pelo DROP PROCEDURE
na linha 387, a solução foi remover a linha extra.

3. TIMEOUT DE 30s ao INSERIR 30.000 VENDAS (Error Code: 2013)
   Erro: A procedure original executava 150.000 instruções SQL
    individuais SEM transacção.
   
   Resoluções:
   1) START TRANSACTION / COMMIT
      -> Reduziu de ~150.000 fsyncs para 1 único flush no final.
   2) Tabela temporária tmp_preco
      -> Pré-carrega os preços dos 200 produtos em memória, eliminando
         90.000 SELECTs individuais à tabela PRODUTO dentro do loop.
   3) Total calculado antes do INSERT
      -> O total da venda é calculado no próprio loop e inserido de uma
         vez, eliminando 30.000 UPDATEs VENDA que antes corrigiam o total.
   Resultado: Script completo em segundos, 30.000 vendas e 90.000 itens.

4. PORQUE STORED PROCEDURES EM VEZ DE INSERT MANUAL?
   - 125 mil INSERTs escritos à mão dá muito trabalho e é difícil de manter
   - Com loops (WHILE) muda-se um número para aumentar a quantidade
   - A procedure é preparada uma só vez, INSERTs soltos o MySQL lê e
     interpreta um por um, o que é mais lento.
    - RAND(), MOD() e ELT() geram dados realistas diferentes a cada execução

═══════════════════════════════════════════
REFLEXÃO ESCRITA — FASE 0
═══════════════════════════════════════════

1. O script gerado pela IA funcionou directamente ou precisou de correcções? Que tipo de erros surgiram e o que aprenderam ao corrigi-los?

Não funcionou de primeira. Achámos dois erros: uma linha que juntou o nome da procedure com o comando SET FOREIGN_KEY_CHECKS, e uma chamada CALL repetida no fim depois da procedure já ter sido apagada. Aprendemos que a IA às vezes alucina e junta coisas que não devia. Agora sabemos: testar sempre o script antes de assumir que está pronto.

2. Porque é que o grupo decidiu usar stored procedures com loops em vez de INSERT linha a linha? Qual a vantagem em termos de tempo e manutenção?

Escrever 125 mil INSERTs manualmente não faz sentido. Com loops, se precisarmos de 50 mil vendas em vez de 30 mil, muda-se um número no WHILE. Além disso, a procedure é compilada uma vez, os INSERTs soltos o MySQL lê um por um — muito mais lento. E com RAND() e ELT() conseguimos dados diferentes de cada vez que corremos.

═══════════════════════════════════════════
REFLEXÃO ESCRITA — FASE 1
═══════════════════════════════════════════

1. O que significa o mapeamento de portas '3307:3306'? Porque é que os nós Benguela e Huambo não podem também usar a porta 3306 no anfitrião?

O lado direito (3306) é a porta dentro do contentor, onde o MySQL está à escuta. O lado esquerdo (3307, 3308) é a porta no computador anfitrião que encaminha para dentro do contentor. Como só uma aplicação pode ocupar uma porta de cada vez no anfitrião, e a 3306 já está a ser usada pelo nó Luanda (ou pelo MySQL local), os outros nós têm de usar portas diferentes para não haver conflito.

2. Qual a vantagem de usar o volume './dados:/docker-entrypoint-initdb.d'? O que aconteceria se não usassem esse volume e quisessem carregar os dados manualmente?

O MySQL corre automaticamente todos os scripts .sql que encontra nessa pasta quando o contentor arranca pela primeira vez. Sem o volume, teríamos de fazer COPY para dentro do contentor e depois executar SOURCE manualmente — mais trabalho e mais propenso a erros. O volume automatiza todo o processo de carga inicial.

3. Se desligarem o computador e voltarem a ligar, os dados permanecem? Testem e documentem o que observam.

Testámos com docker restart no-luanda e os dados continuam lá: 30000 vendas. Mas atenção: se fizermos docker compose down (que apaga os contentores) e depois up, os dados perdem-se porque não configurámos volumes persistentes para o MySQL. O script volta a carregar os dados do início porque a pasta ./dados está montada como init. Para ter dados realmente persistentes, seria preciso um volume como ./mysql-data:/var/lib/mysql.

═══════════════════════════════════════════
REFLEXÃO ESCRITA — P1-A e P1-B
═══════════════════════════════════════════

1. O Slave conseguiu acompanhar o Master em tempo real? Qual foi o atraso máximo observado?

O Seconds_Behind_Master ficou sempre em 0. Na prática, os 5 novos produtos que inserimos estavam visíveis no slave antes de conseguirmos medir — replicação praticamente instantânea. Isto porque estávamos em rede local, sem carga extra.

2. Como verificaram que a soma dos fragmentos corresponde ao total geral? Explique o raciocínio.

Corremos SELECT COUNT(*) da VENDA em cada fragmento (Luanda lojas 1-5, Benguela 6-10, Huambo 11-15) e somámos: 10012 + 9937 + 10052 = 30001, que é exactamente o COUNT(*) da VENDA no master. Isto confirma que a fragmentação horizontal está correcta — nenhuma venda se perdeu nem duplicou.

3. Qual a província com maior faturamento médio por cliente e o que isso sugere sobre o negócio?

Benguela, com 50.376 Kz por cliente. Isto sugere que os clientes de Benguela compram mais por transacção, embora Luanda tenha mais vendas no total. Pode ser um mercado de maior poder de compra ou simplesmente menos clientes mas mais fiéis.

═══════════════════════════════════════════
REFLEXÃO ESCRITA — P2-A e P2-B
═══════════════════════════════════════════

1. O que aconteceu ao stock e às vendas quando o erro foi forçado? O ROLLBACK funcionou como esperado?

Sim. Forçámos um erro depois de decrementar o stock em Benguela e inserir a venda em Luanda. O ROLLBACK reverteu ambas as operações: o stock voltou a 88 e as vendas a 30002. Se não houvesse transacção distribuída, o stock ficava descontado sem venda correspondente.

2. Na transferência entre Huambo e Benguela, o que aconteceu quando o stock era insuficiente? O ROLLBACK foi automático?

Sim. Quando pedimos para transferir 9999 unidades (muito acima do stock disponível em Huambo, que era 141), o script detectou stock insuficiente antes de qualquer UPDATE. Fez ROLLBACK em ambos os nós e nenhum dos stocks foi alterado. O sistema manteve-se consistente.

═══════════════════════════════════════════
REFLEXÃO ESCRITA — P3-A e P3-B
═══════════════════════════════════════════

1. O MongoDB foi mais rápido que o MySQL nas agregações? Porquê?

Sim, o MongoDB foi mais rápido nas duas queries: total por loja (45 ms contra 55 ms) e top 10 clientes (19 ms contra 61 ms). O MongoDB guarda os dados num formato documental que evita JOINs — a venda já vem com os itens dentro. No MySQL, a agregação precisa de percorrer VENDA e ITEM_VENDA separadamente. A diferença é mais notória em agregações complexas.

2. Os resultados do MongoDB coincidiram com os do MySQL? O que isso prova?

Sim, o top 1 cliente foi o mesmo (António Gomes) e os totais por loja bateram certo. Isto prova que a migração foi correcta: mesmo sem constraints, o MongoDB tem os mesmos dados e produz as mesmas respostas.

3. Para o MercadoKwanza, recomendaria MySQL ou MongoDB? Em quê?

Os dois. MySQL para as transacções do dia-a-dia (vender, actualizar stock) porque precisa de ACID. MongoDB para relatórios e agregacões porque é mais rápido e mais flexível. Um sistema híbrido tira partido do melhor de cada um.
